import styled from 'styled-components';

const CardSectionWrapper = styled.main `
`;

const Card = styled.main `
    padding: 30px;
    margin-bottom: 60px;
`;

export {
  CardSectionWrapper,
  Card,
};